import { Routes, RouterModule } from '@angular/router';
import { CompanyManagerComponent } from './company-manager.component';

export const companyRoutes: Routes = [
  { path: '', component: CompanyManagerComponent },
];
