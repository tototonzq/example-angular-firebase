import { Routes, RouterModule } from '@angular/router';
import { ResponseFormComponent } from './response-form.component';

export const ResponseRoutes: Routes = [
  { path: '', component: ResponseFormComponent },
];
