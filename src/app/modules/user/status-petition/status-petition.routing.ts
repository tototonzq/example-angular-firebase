import { Routes, RouterModule } from '@angular/router';
import { StatusPetitionComponent } from './status-petition.component';

export const statusPetitionRoutes: Routes = [
  { path: '', component: StatusPetitionComponent },
];
